# GitHub Pages Jekyll config — exclude folders that should not be processed by Jekyll
# This prevents Jekyll from trying to render backend/node_modules and similar folders
exclude:
  - backend
  - node_modules
  - .github
  - README.md
  - backend/.env
  - backend/package.json
  - backend/node_modules
